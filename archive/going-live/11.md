Topic 11: **Platform Observability & Trust Signals**.
This module is designed to bridge the "Trust Deficit" for international clients and provide the technical team with the visibility needed to manage a global financial platform.

---

### **Task 1: Integrate Sentry for Frontend Error Tracking**
- **suggested-assignee**: Frontend
- **priority**: High
- **consideration**: Ensure the `environment` tag is set (dev vs. prod) so local development errors don't clutter the production alerts.
- **description**: Implement Sentry to catch and report runtime JavaScript errors and failed API calls in the browser.
- **requirements**:
    1. Install `@sentry/react`.
    2. Initialize Sentry in `main.tsx` using a DSN from environment variables.
    3. Add a "User Feedback" dialog trigger for hard crashes.
- **acceptance-criteria**:
    - Purposely throwing an error in a component triggers an alert in the Sentry dashboard with a full stack trace.

---

### **Task 2: Integrate Sentry for Go Backend**
- **suggested-assignee**: Backend
- **priority**: High
- **consideration**: Use Sentry's Gin middleware to automatically capture panics and 500-level errors.
- **description**: Monitor server-side crashes, especially during critical operations like payment webhooks or media processing.
- **requirements**:
    1. Install `sentry-go`.
    2. Initialize in `main.go`.
    3. Use `sentrygin.New()` middleware in `router.go`.
- **acceptance-criteria**:
    - Backend panics are reported to Sentry with request context (method, path, user ID).

---

### **Task 3: Public Status Page Implementation**
- **suggested-assignee**: Junior
- **priority**: Medium
- **consideration**: Use a specialized service like **BetterStack** or **Cronitor** rather than building it from scratch. It builds trust if the status page is independent of your main server.
- **description**: Set up a public-facing page (e.g., `status.zeyaroo.com`) to show system uptime.
- **requirements**:
    1. Configure monitors for the API URL and the Frontend URL.
    2. Add a link to the status page in the app footer.
- **acceptance-criteria**:
    - Users can view real-time uptime stats for "API," "Storage Engine," and "Payments."

---

### **Task 4: Add "Verified Creator" Field to DB**
- **suggested-assignee**: Backend
- **priority**: High
- **consideration**: Verification should be a manual "Zeyaroo Stamp of Approval" initially to prevent scammers from looking legitimate.
- **description**: Add a boolean flag and a "Verified At" timestamp to creator profiles.
- **requirements**:
    1. Update `models.CreatorProfile` to include `IsVerified` (bool) and `VerifiedAt` (timestamp).
    2. Add this field to the public profile API response.
- **acceptance-criteria**:
    - The database can distinguish between verified and unverified creators.

---

### **Task 5: "Verified Badge" UI Implementation**
- **suggested-assignee**: Frontend
- **priority**: High
- **consideration**: Use a distinct icon (e.g., a checkmark in a shield) that communicates "Vetted by Zeyaroo."
- **description**: Display the verification badge on the Creator Profile and Proposal pages.
- **requirements**:
    1. Create a `VerifiedBadge.tsx` component.
    2. Render it next to the creator's name if `is_verified` is true.
    3. Add a tooltip: "This creator has been vetted by Zeyaroo for identity and quality."
- **acceptance-criteria**:
    - Clients see the badge prominently on the proposal, increasing confidence to fund the escrow.

---

### **Task 6: Transparency Stats API (Social Proof)**
- **suggested-assignee**: Backend
- **priority**: Medium
- **consideration**: Cache this query. Running a `COUNT(*)` on a million media rows every time the homepage loads is expensive.
- **description**: Create a public endpoint to provide aggregate platform stats for the landing page.
- **requirements**:
    1. Endpoint: `GET /api/v1/public/stats`.
    2. Return: `total_volume_secured` (sum of completed milestones), `total_files_delivered`, and `active_creators`.
- **acceptance-criteria**:
    - The landing page can display "Over $10,000 secured in escrow" using real-time (cached) data.

---

### **Task 7: "Active Since" & "Completed Projects" Badges**
- **suggested-assignee**: Fullstack
- **priority**: Medium
- **consideration**: "Time on platform" is a proxy for reliability.
- **description**: Show history stats on a creator's public profile.
- **requirements**:
    1. Calculate "Member Since" based on user creation date.
    2. Count projects where status is `completed`.
    3. Display these stats in a "Trust Box" on the profile.
- **acceptance-criteria**:
    - Profile shows: "Member since 2024 • 15 Projects Completed."

---

### **Task 8: Media Processing Health Check**
- **suggested-assignee**: Backend
- **priority**: Medium
- **consideration**: If the thumbnail worker dies, creators think their uploads failed.
- **description**: Create a monitor for the background media processing queue.
- **requirements**:
    1. Track "Processing" vs "Completed" counts in the `media` table.
    2. If a file is "Processing" for more than 30 minutes, trigger an alert to the team.
- **acceptance-criteria**:
    - Team receives a notification if the media processing pipeline is backed up.

---

### **Task 9: User "Security Log" View**
- **suggested-assignee**: Fullstack
- **priority**: Low
- **consideration**: Helps users feel in control of their account security.
- **description**: Allow users to see a history of logins and sensitive actions (like payout detail changes).
- **requirements**:
    1. Create `models.AuditLog`.
    2. Record IP address and User Agent for every login.
    3. Show this list in `Settings -> Security`.
- **acceptance-criteria**:
    - Users can verify that no unauthorized IPs have accessed their account.

---

### **Task 10: "Escrow Explained" Help Widget**
- **suggested-assignee**: Frontend
- **priority**: High
- **consideration**: This is a direct conversion tool for skeptical clients.
- **description**: Add a "How this works" info-popup next to the milestone "Pay" button.
- **requirements**:
    1. Create a modal/popover explaining the 3 steps: 1. You Pay, 2. We Hold, 3. You Release after review.
- **acceptance-criteria**:
    - A client can click "What is Escrow?" and understand the safety of the platform in under 30 seconds.

---

### **Task 11: Bug Report / Feedback Widget Integration**
- **suggested-assignee**: Junior
- **priority**: Low
- **consideration**: Use a tool like **Doorbell.io** or **HubSpot Free**. 
- **description**: Provide a low-friction way for users to report issues directly from the dashboard.
- **requirements**:
    1. Embed a floating "Feedback" button.
- **acceptance-criteria**:
    - Submitting feedback sends an email to the Zeyaroo support alias.

---

### **Task 12: Automatic "Maintenance Mode" Toggle**
- **suggested-assignee**: Senior
- **priority**: Medium
- **consideration**: Essential for database migrations or major upgrades.
- **description**: A way to disable the API and show a "Maintenance" screen to all users.
- **requirements**:
    1. Add an `MAINTENANCE_MODE` environment variable.
    2. Middleware that returns `503 Service Unavailable` if active.
- **acceptance-criteria**:
    - Enabling the toggle prevents all writes and shows a clear "We'll be back soon" message.

---

**Final Topic?**
Shall we conclude with **Topic 12: Creator Productivity (AI & Roadmap)**? This covers the "AI Culling" and long-term storage archiving logic.