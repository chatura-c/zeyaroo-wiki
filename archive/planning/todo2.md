create firebase projects for 
- dev
- prod

~~when user account does not exist in the database, the login page is stuck in a loop, calling /api/v1/auth/firebase/login and api responds 401~~ ✓ fixed

create a seperate .env file for local development

add a default cover image for the studio project

everyone should be able to upload a profile photo regardless of the subscription

enforce subscription limits. make so that we are able to customize the package per user.

profile slug. universal profile slug that can be claimed by creators. may be with a custom domain feature. so a premium feature

are uploaded media being post processed -- create thumbnails etc.

in project hub gallery the top left check box does not work. it should select the photo same effect as pressed ctrl + click

remove eruda

in inquiry dialog show only the categories that the creator has added in his profile. plus 'other' category

integrate real emails - resend (credentials moved to production .env — RESEND_API_KEY)

in proposal builder chapters, and timeline in logistics dates should be date fields so the datepicker popsup

